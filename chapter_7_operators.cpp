#include <iostream>
using namespace std;

int main() {
  int a = 5, b = 10, c = 15, d = 20;
  
  cout << "a + b = " << (a + b) << endl;
  cout << "d - c = " << (d - c) << endl;
  cout << "a * b = " << (a * b) << endl;
  cout << "d / a = " << (d / a) << endl;

  return 0;
}