#include <iostream>
#include <cmath>
using namespace std;

int main() {
  // Initialize a double for input
  double num;

  cout << "Please enter a number: ";
  cin >> num;

  // Calculate the square root of the number
  cout << "The square root of " << num << " is " << sqrt(num) << endl;
  
  return 0;
}